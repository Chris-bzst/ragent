# Required: Claude API Key
CLAUDE_API_KEY=your_claude_api_key_here

# Optional: Custom API endpoint (for proxies or regional endpoints)
# CLAUDE_BASE_URL=https://api.anthropic.com

# Optional: Server port (default: 3001)
# PORT=3001

# Optional: HTTP Basic Authentication (recommended for cloud deployment)
# AUTH_USERNAME=admin
# AUTH_PASSWORD=your_secure_password_here

# Optional: Shared terminal session name
# TMUX_SESSION_NAME=claude-workspace

# Optional: WebSocket origin allowlist (comma-separated)
# ALLOWED_ORIGINS=http://localhost:3001
