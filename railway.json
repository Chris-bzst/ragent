{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "docker/Dockerfile"
  },
  "deploy": {
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  },
  "environments": {
    "production": {
      "variables": {
        "CLAUDE_API_KEY": {
          "description": "Your Anthropic API key (optional — if not set, log in via OAuth by running 'claude' in the terminal)",
          "required": false
        },
        "AUTH_USERNAME": {
          "description": "HTTP Basic Auth username to protect your instance",
          "required": false
        },
        "AUTH_PASSWORD": {
          "description": "HTTP Basic Auth password to protect your instance",
          "required": false
        },
        "CLAUDE_BASE_URL": {
          "description": "Custom API endpoint (optional, for proxies or regional endpoints)",
          "required": false
        },
        "TMUX_SESSION_NAME": {
          "description": "Enable shared terminal sessions across browser tabs (set any value to enable)",
          "required": false
        }
      }
    }
  }
}
